ОС 20.04.2 LTS

1. страница на гитхаб
https://github.com/pyenv/pyenv-installer


2. выполнить
curl https://pyenv.run | bash

3. открыть настройки терминала
sudo  vim  ~/.bashrc    

4. внести в начало
export PATH="~/.pyenv/bin:$PATH"
eval "$(pyenv init -)"
eval "$(pyenv virtualenv-init -)"

5. перезагрузить терминал
source ~/.bashrc

6.проверка
pyenv versions
pyenv install --list

7.установить версию
pyenv install 3.6.9

6.перейти на другую версию
pyenv global 3.6.9

8.выполнить, если не полчается
eval "$(pyenv init --path)"

7.проверка
pyenv versions

8.установить локально, сначало перейти в папку проекта, потом:
pyenv local 3.6.9

9.Перейти на системный python 
pyenv install system


sudo apt-get install build-essential
sudo apt-get install zlib1g-dev

